# Changelog — respec-software

All notable changes to the marketing site (respecsoftware.com).

## 2026-07-02

- `993ae55` Show fallback contact info (phone + both emails) when the contact form webhook fails, instead of silently showing "success" on a lost lead.
- `2a98e36` Added a real 1200x630 `og-image.png` social share banner, replacing the square favicon in `og:image`/`twitter:image`.
- `3661695` Added phone number (512) 569-5306 as click-to-call in the contact section, footer, and structured data.
- `f563e87` Added SEO fundamentals: canonical URL, Open Graph/Twitter tags, `ProfessionalService` JSON-LD, improved title/meta description, semantic `<h3>` headings, `robots.txt`, `sitemap.xml`. Marked `demo/`, `hud/`, `intake/` as `noindex` (sales demos, not content pages).
- `d96d5b7` Added favicon set (ico/svg/apple-touch/PNG sizes) and a generated `site.webmanifest`.
- `f4137a7` Replaced `demo/`, `hud/`, `intake/` with updated versions adding tour navigation.
